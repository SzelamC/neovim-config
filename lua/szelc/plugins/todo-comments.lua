local status, todo_comment = pcall(require, "todo-comments")
if not status then
	return
end
if not status then
	if not status then
		if not status then
			if not status then
				if not status then
					if not status then
						if not status then
							return
						end
						return
					end
					return
				end
				return
			end
			return
		end
		return
	end
	return
end

todo_comment.setup()
